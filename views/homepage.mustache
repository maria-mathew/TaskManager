<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Task Manager Application</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .task-tile { 
      border: 1px solid #ddd; 
      padding: 15px; 
      margin-bottom: 15px; 
      background-color: #f2f4f1; 
      border-radius: 5px;
      display: flex;
      flex-direction: column;
      min-height: 200px;
    }
    .task-title { font-weight: bold; }
    .task-tile p {
      margin-bottom: 5px;
    }
    .btn-container {
      margin-top: auto;
    }
  </style>
</head>

<body style="background-color: #dee2dc;">

  <!-------------- navigation bar ------------->
  <nav class="navbar navbar-expand-md navbar-dark" style="background-color: #4a5444;">
    <div class="container-fluid">
      <a class="navbar-brand text-white" href="/">Task Manager Application</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item me-2">
            <a class="nav-link text-white btn btn-dark" href="/addform">Create Task</a>
          </li>
          <li class="nav-item">
            <a class="nav-link text-white btn btn-dark" href="/tasks">Tasks ListView</a>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <div class="container mt-4">
    <h3 class="text-center mb-5">Tasks By Status</h3>

    <!------------------ to do task list ---------------->
    <div class="row">
      <div class="col-md-4">
        <h5 class="text-center">To-do</h5>
        {{#tasks.todo}}
          <div class="task-tile">
            <div class="task-title" style="color: {{#isOverdue}}red{{/isOverdue}}{{^isOverdue}}black{{/isOverdue}}">
              {{title}}
            </div>
            <div class="mb-1">{{description}}</div>
            <hr class="my-2">   
            <div>Due Date: {{dueDate}}</div>
            <div>Category: {{category}}</div>
            <div>Priority: {{priority}}</div>
            <div>Status: {{status}}</div>
            <div class="btn-container d-flex justify-content-end">
              <a href="/deletetask/{{rowid}}" class="btn btn-dark btn-sm me-2">Delete</a>
              <a href="/updateform/{{rowid}}" class="btn btn-secondary btn-sm">Update</a>
            </div>
          </div>
        {{/tasks.todo}}
      </div>
      
      <!------------------ inprogress task list ---------------->
      <div class="col-md-4">
        <h5 class="text-center">In Progress</h5>
        {{#tasks.inProgress}}
          <div class="task-tile">
            <div class="task-title" style="color: {{#isOverdue}}red{{/isOverdue}}{{^isOverdue}}black{{/isOverdue}}">
              {{title}}
            </div>
            <div class="mb-1">{{description}}</div>
            <hr class="my-2">   
            <div>Due Date: {{dueDate}}</div>
            <div>Category: {{category}}</div>
            <div>Priority: {{priority}}</div>
            <div>Status: {{status}}</div>
            <div class="btn-container d-flex justify-content-end">
              <a href="/deletetask/{{rowid}}" class="btn btn-dark btn-sm me-2">Delete</a>
              <a href="/updateform/{{rowid}}" class="btn btn-secondary btn-sm">Update</a>
            </div>
          </div>
        {{/tasks.inProgress}}
      </div>
      
      <!------------------ completed task list ---------------->
      <div class="col-md-4">
        <h5 class="text-center">Completed</h5>
        {{#tasks.completed}}
          <div class="task-tile">
            <div class="task-title" style="color: {{#isOverdue}}red{{/isOverdue}};">
              {{title}}
            </div>
            <div class="mb-1">{{description}}</div>  
            <hr class="my-2">           
            <div>Due Date: {{dueDate}}</div>
            <div>Category: {{category}}</div>
            <div>Priority: {{priority}}</div>
            <div>Status: {{status}}</div>
            <div class="btn-container d-flex justify-content-end">
              <a href="/deletetask/{{rowid}}" class="btn btn-dark btn-sm me-2">Delete</a>
              <a href="/updateform/{{rowid}}" class="btn btn-secondary btn-sm">Update</a>
            </div>
          </div>
        {{/tasks.completed}}
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
